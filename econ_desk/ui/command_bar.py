# Command input bar with tab auto-completion.
# Handles user commands such as "view <asset>", "refresh", "back", "quit".
# Tab cycles through matching instrument names for auto-completion.

from textual.widgets import Input
